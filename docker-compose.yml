services:
  postgres:
    build:
      context: .
      dockerfile: Dockerfile.postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: cylaw
      POSTGRES_USER: cylaw
      POSTGRES_PASSWORD: cylaw_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
